# TECNIFIN — instrucciones para agentes

Las reglas del proyecto, los comandos y las trampas de Windows están en `CLAUDE.md`: **leerlo primero y seguirlo tal cual**.
El plan y el orquestador viven en `C:\GUTT_SYSTEM`. No tocar el sistema vigente (SQL Server `SQLGUTPATATE`, `server.js`).
